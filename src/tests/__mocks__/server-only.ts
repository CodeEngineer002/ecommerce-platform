// server-only mock for test environment.
// This module only exists to throw at import time in browsers / client bundles.
// In tests (Node.js) it is safe to import — this mock is a no-op.
export {};
